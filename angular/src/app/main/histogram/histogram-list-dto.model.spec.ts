import { HistogramListDto } from './histogram-list-dto.model';

describe('HistogramListDto', () => {
  it('should create an instance', () => {
    expect(new HistogramListDto()).toBeTruthy();
  });
});
